# Copy to .env.local for development. .env* is gitignored.

# Postgres connection. Matches docker-compose.yml service defaults.
DATABASE_URL=postgres://pulse:pulse@localhost:5432/pulse

# Set this to your own user id for single-user dev mode. v0.2 replaces
# this with real auth.
# PULSE_DEV_USER=mason
